# Decode String

echo [base64 encoded string] | base64 -d

# Encode String

echo [string] | base64

# Encode String without wrapping lines 

echo [string] | base64 -w 0

-w 0 - means do not wrap output lines
